# Public URL of your API (browser-safe — must start with NEXT_PUBLIC_)
# Copy to ".env.local" for local dev: cp .env.example .env.local
# On Vercel: Project → Settings → Environment Variables → add the same name/value
NEXT_PUBLIC_API_URL=https://client-flow-api.vercel.app/api/v1
